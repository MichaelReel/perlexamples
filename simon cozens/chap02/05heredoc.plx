#!/usr/bin/perl
use warnings;

print<<EOF;

The marker "EOF" above can be anything. EOF is simply a convention. There is
different text in the tutorial PDF, but the PDF is locked for copying and I
cannot be bothered to type all that nonsense in.

EOF
